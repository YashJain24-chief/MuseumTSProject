1. Change the folder structure
2. Creating constants for action types and creating separate actions instead of passing object directly inside dispatch 
3. Moving useSelector to a separate file - solved this using custom hooks
4. Navigating to details screen only when data is available
5. Move Styling to separate file
6. Renamed Saga files to something more redable
7. Convert to typescript
